# Bifurcate — pinned so the environment reproduces in one command.
# The cipher core (bifurcate/) needs ONLY the standard library;
# everything below serves RSA, the GUI, the visuals, or the tests.
cryptography>=42.0
streamlit>=1.30
plotly>=5.18
pillow>=10.0
numpy>=1.26
pytest>=8.0
